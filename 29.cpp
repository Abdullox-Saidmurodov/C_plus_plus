#include <bits/stdc++.h>
using namespace std;
int main ()
{
	int a;
	double x,y,BB1;
	cin>>a>>x>>y;
		BB1=sqrt(y*y+pow(M_E,x)+sqrt(pow(M_E,x)+a/(x*x+2))+cos(x)*cos(x)/sin(x*x))+cos(x)*cos(x)*cos(x);
	cout.precision(2);
	cout<<fixed<<BB1;
	return 0;
	}	
